# 需要加工的数据 — 逐项对照表

> 需求原文：`notion_schema/需要加工的数据.md`  
> 实现脚本：`sql/run_all_20260715.sql`（Part 4 报告扩展 → Part 6 宽表）  
> 终表字段：`jcr_credit_feature_20260715` / `jcr_credit_feature_label_full_20260715`

---

## 总览

| 结论 | 说明 |
|------|------|
| **需求文档列出的字段** | **已全部实现**（含资质：房贷 / 公积金贷款 / 单位类型） |
| **易误解点** | 资质在需求里只有 **0/1 标志 + 单位类型编码**，不含公积金缴存额、单位名称等 |
| **可选未做** | 账单日「相同账单日最大账户数」的近 1 月 / 6 月 / 1 年版（需求写「可以加」） |
| **看到 0 或 null** | 多半是 **人无该业务或该报告段为空**，不是字段没加工 |

---

## 逐行对照

### 1. 循环贷额度及额度利用率（剔马消 `T10156530H0001`）

| 需求（详细特征） | 宽表字段 | 状态 |
|------------------|----------|------|
| 最近一份 / 近一月 / 近6月 / 近一年 **有余额账户数** | `latest_pos_bal_acct_cnt`, `avg_1m/6m/1y_pos_bal_acct_cnt` | ✅ |
| 最近一份 **总/最大/最小余额** + 近 1/6/12 月均值 | `latest_bal_*`, `avg_1m/6m/1y_bal_*` | ✅ |
| 最近一份 **授信额度** + 近 1/6/12 月均值 | `latest_crdt_*`, `avg_1m/6m/1y_crdt_*` | ✅ |
| 最近一份 **额度利用率** + 近 1/6/12 月均值 | `latest_util_*`, `avg_1m/6m/1y_util_*` | ✅ |

**源表**：`loan_account_latest_perform` + `loan_account_basic_info`（Part 3）→ 报告聚合（Part 4）

---

### 2. 账单日

| 需求 | 宽表字段 | 状态 |
|------|----------|------|
| 最近一份：账单日总数 | `latest_bill_day_cnt` | ✅ |
| 最近一份：相同账单日最大账户数 | `latest_same_billday_acct_cnt_max` | ✅ |
| 相同账单日下最大余额（近 1/6/12 月） | `max_1m/6m/1y_same_billday_bal_sum` | ✅ |
| 相同账单日最大账户数（近 1/6/12 月） | — | ⚠️ 需求写「可以加」，**未实现** |

---

### 3. 征信查询次数（循环贷历史借贷表现）

| 需求（江承润 7 项） | 宽表字段（最近一份） | 近一年多报告均值 |
|---------------------|----------------------|------------------|
| 近一月查询机构数（信用卡审批） | `latest_credit_audit_query_org_num_1m` | `avg_1y_credit_audit_query_org_num_1m` |
| 近一月查询次数（贷款审批） | `latest_loan_audit_query_num_1m` | `avg_1y_loan_audit_query_num_1m` |
| 近一月查询次数（信用卡审批） | `latest_credit_audit_query_num_1m` | `avg_1y_credit_audit_query_num_1m` |
| 近一月查询次数（本人查询） | `latest_person_query_num_1m` | `avg_1y_person_query_num_1m` |
| 近两年查询次数（贷后管理） | `latest_plm_query_num_2y` | `avg_1y_plm_query_num_2y` |
| 近两年查询次数（担保资格审查） | `latest_assure_query_num_2y` | `avg_1y_assure_query_num_2y` |
| 近两年查询次数（特约商户实名审查） | `latest_sam_query_num_2y` | `avg_1y_sam_query_num_2y` |

额外衍生：`latest_hard_query_num_1m` / `avg_1y_hard_query_num_1m`（贷款+信用卡审批合计）

**源表**：`dsst_eds_gaa02_query_summary`（Part 4）

---

### 4. 逾期次数 / 逾期金额（最近一份）

| 需求 | 宽表字段 | 状态 |
|------|----------|------|
| 逾期月份数 | `latest_pd_num_month` | ✅ |
| 总共逾期次数 | `latest_pd_total_overdue_cnt` | ✅ |
| 最长逾期/透支月数 | `latest_pd_max_overdue_months` | ✅ |
| 单月最高逾期/透支金额 | `latest_pd_max_overdue_amt` | ✅ |

**源表**：`credit_loan_summary_pd_summary`（字段 `max_amt_pd` 作最长逾期月数）

---

### 5. 资质相关 ⭐

需求原文（`需要加工的数据.md` 第 13～15 行）**只列了下面 3 项**：

| 需求原文 | 特征列名（Notion 表） | 宽表字段 | 状态 | SQL 实现要点 |
|----------|----------------------|----------|------|--------------|
| **是否有房贷**，取最近一份 | 公积金（大类：资质相关） | `latest_has_house_loan_flg` | ✅ | `tip_detail` 业务类型 11/12；`basic_info` 住房类；取最近一份报告 |
| **是否有公积金贷款**，取最近一份 | 同上 | `latest_has_gjj_loan_flg` | ✅ | `tip_detail` 类型 13；`basic_info` 公积金；`phf_record` 有参缴记录 |
| **工作单位类型**，取最近一份 | 单位类型 | `latest_org_type` | ✅ | `person_job_info.org_type`，同报告 `row_number` 取一条 |

**不在需求文档内、故未加工**（Notion 字典里有，但 `需要加工的数据.md` 没要求）：

- 公积金月缴存额、缴存比例、参缴状态（`phf_record` 明细）
- 工作单位名称 `company_name`（只有 `org_type`）
- 职业、行业等其它公共信息

**你上条样例**：`latest_has_house_loan_flg=0`, `latest_has_gjj_loan_flg=0`, `latest_org_type=99` → **字段有值**（无房贷/无公积金贷款/单位类型编码 99），不是缺列。

---

### 6. 信用卡（最近一份）

| 需求 | 宽表字段 | 状态 |
|------|----------|------|
| 账户数 | `latest_credit_account_num` | ✅ |
| 额度 | `latest_credit_amount` | ✅ |
| 余额 | `latest_credit_used_amount` | ✅ |
| 额度利用率 | `latest_credit_util_rate` | ✅ |

**源表**：`credit_loan_summary` 贷记卡汇总段（`credit_account_num` 等）

**null 常见原因**：该用户无贷记卡汇总段 / 最近一份报告无信用卡账户。

---

## 资质类填充率核验 SQL

```sql
select
    count(1) as n,
    round(avg(if(latest_has_house_loan_flg is not null, 1, 0)), 4) as rate_house_flg,
    round(avg(if(latest_has_gjj_loan_flg is not null, 1, 0)), 4) as rate_gjj_flg,
    round(avg(if(latest_org_type is not null, 1, 0)), 4) as rate_org_type,
    round(avg(latest_has_house_loan_flg), 4) as pct_has_house,
    round(avg(latest_has_gjj_loan_flg), 4) as pct_has_gjj
from lj_iceberg.ai_decision_dev.jcr_credit_feature_label_full_20260715
where m in ('202508', '202509', '202510')
  and label_eligible = 1;
```

若 `rate_*_flg` 接近 1 但 `pct_has_house` 很低，说明 **大多数人没有房贷**，不是字段缺失。

---

## 与更广 Notion 字典的区别

`notion_schema/` 下还有 30+ 张征信子表（强制执行、欠税、执业资格等），**均不在** `需要加工的数据.md` 的加工范围内。  
当前 SQL 只覆盖该文档明确列出的特征大类。
