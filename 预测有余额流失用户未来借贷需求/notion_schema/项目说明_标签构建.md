# 项目说明：标签构建（Label）

## 一、业务目标

预测 **当前有效额度 ≥ 2 万元且有余额** 的用户，在样本日后是否会产生 **外部征信余额增加**（有再借贷/加杠杆需求）。

---

## 二、样本定义

### 2.1 入模样本池

| 项目 | 说明 |
|------|------|
| **时间** | 2025 年 10 月（`dt` 20251002~20251101，分区延后 1 天） |
| **用户条件** | `prod_cd=5103` 复贷有余额，`rk=1` 月内最低额度利用率日 |
| **`days_dt_1`** | `date_sub(days_dt, 1)`（had/with/label 锚点） |

### 2.2 分析 cohort（精确 **5401**）

**权威口径**：同事最后一查在 `yye_pril_bal_info_20260623_4` 上 `group by uuid`，不是 `_2` 上直接 WHERE。

| 方式 | 人数 | 是否权威 |
|------|------|----------|
| `jcr_pril_bal_info` 同条件 WHERE（自建 Step0） | ~5186 | ✗ 差 215 |
| `jcr_cohort_5401_20260623`（Step 0d 从 yye _4） | **5401** | ✓ |

**`cohort_eligible = 1`**：`uuid` 在 `jcr_cohort_5401_20260623` 中。

**执行顺序（精确 5401）**：
1. `step0c_copy_from_yye.sql` — 复制同事 `_2`
2. `step0d_cohort_5401_from_yye.sql` — 从同事 `_4` 取 5401 个 uuid
3. Step 5~7（inner join cohort 表）

差异诊断：`sql/diagnose_cohort_diff.sql`

---

## 三、标签定义

| 标签值 | 含义 | 判定逻辑 |
|--------|------|----------|
| **正样本 `label = 1`** | 有需求 | `fwd_max_balance > fwd_first_balance` |
| **负样本 `label = 0`** | 无需求 | forward 60 天余额未增加 |

- `fwd_first_balance`：`days_dt_1~+60` 内最早征信日（`zx_rank=1`）非马消循环贷余额合计
- `fwd_max_balance`：同窗口内最大余额合计
- 剔马消：`org_manage_code <> 'T10156530H0001'`

---

## 四、数据集划分

| 数据集 | 范围 | 用途 |
|--------|------|------|
| train + val | `sample_month='202510'`，hash 80/20 | 训练 |
| test | `days_dt='2025-11-01'` | 离线评估 |

---

## 五、相关表

| 表名 | 说明 |
|------|------|
| `jcr_pril_bal_info_20260623` | 样本表（Step 0a~0c） |
| `jcr_credit_feature_20260623` | 特征宽表 |
| `jcr_credit_feature_label_20260623` | 特征 + 标签 + `cohort_eligible` |

核验脚本：`sql/verify_cohort_5401.sql`
