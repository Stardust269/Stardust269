# 项目说明：三月样本（20260715）

> 同事参考：`sql/yye_pril_bal_sample_reference_20260715.sql`（示例，含 `*_5103` 字段，我方不用）  
> 我方实现：`sql/run_all_20260715.sql`（**仅全渠道无余额 + 全渠道 with_***）

---

## 一、三步漏斗

| 步骤 | 条件 | 关键字段 |
|------|------|----------|
| **① 入口** | 5103 有余额 + `crdt>=2w` + 月内 rk=1 | `prod_cd='5103'` |
| **② 流失+未贷** | **全渠道** 60 天内无余额 + **全渠道**未提现 | `no_balance_flg_60=1`，`with_0_30+with_31_60=0` |
| **③ 征信** | 0-30 / 31-60 均有征信 | `had_0_30_zx=1`，`had_31_60_zx=1` → 10月 ~5401 |

**不用**：`no_balance_flg_*_5103`、`with_*_5103`（已从 jcr 脚本移除）

### 锚点

- `no_balance`：`days_dt`
- `had` / `with` / 征信特征 / 标签：`days_dt_1`

---

## 二、样本终表 `jcr_pril_bal_info_20260715`

对应同事 `_2` 表的**全渠道子集**，字段：

- `no_balance_flg_30/60/90`
- `with_0_30` … `with_91_120`
- `had_*_zx`

马消特征、征信特征均在 **cohort 子集**上加工（Part 3~7）。

---

## 三、核验

```sql
select count(1) from jcr_pril_bal_info_raw_20260715 where rk=1;     -- ~502w
select count(1) from jcr_pril_bal_pf_20260715;                      -- 全渠道无余额
select m, count(1) from jcr_cohort_20260715 group by m order by m; -- 10月 ~5401
```

---

## 四、执行

```text
drop_all_jcr_tables_20260715.sql → run_part01_sample_cohort_20260715.sql → run_all Part3~8
```
