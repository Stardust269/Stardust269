# 项目说明：三月样本（20260715）

> 同事参考脚本：`sql/yye_pril_bal_sample_reference_20260715.sql`（**5103 单渠道**，马消用）  
> 我方征信特征脚本：`sql/run_all_20260715.sql`（**全渠道**）  
> 十月单 cohort 5401 版：`sql/run_all_20260623.sql`（勿混用）

---

## 一、渠道口径（重要）

| 脚本 | 渠道 | 10 月 cohort 预期 |
|------|------|-------------------|
| 同事 `yye_pril_bal_sample_reference_20260715` | `prod_cd = '5103'` | ~7265（马消/单产品） |
| 我方 `run_all_20260715` | **全渠道**（无 prod_cd 限制） | **~5401**（征信 cohort） |

同事 5103 是「从 ~512w 全渠道里筛有 5103 产品的用户」；征信 cohort 应对齐全渠道五条件，与 0623 十月 5401 同口径。

---

## 二、样本 Step 0a/0b（全渠道）

- **0a**：`base_df`，`sx_rowid=1`，复贷有余额，`crdt_lim_yx>0`，**不限 prod_cd**
- **用户-日聚合**：同日多产品 `sum(pril_bal)` + `max(crdt_lim_yx)`，再按月 `rk=1`
- **0b**：无余额右表同样**全渠道**（去掉 prod_cd）
- **cohort 提现**：`with_0_30 + with_31_60 = 0`（全渠道）；`with_*_5103` 仅保留作马消对照

---

## 三、cohort 五条件（与 0623 一致）

```sql
crdt_lim_yx >= 20000
AND had_0_30_zx = 1 AND had_31_60_zx = 1
AND no_balance_flg_60 = 1
AND with_0_30 + with_31_60 = 0
```

---

## 四、核验 SQL

```sql
select m, count(1) as cohort_cnt
from lj_iceberg.ai_decision_dev.jcr_cohort_20260715
group by m order by m;
-- 10 月 m='202510' 预期 ~5401

select count(1) from lj_iceberg.ai_decision_dev.jcr_cohort_20260715 where m = '202510';
```

---

## 五、执行顺序

```text
Part 0~2：0a(全渠道) → 0b → 0pf → 0c-had → 0c-with → cohort
Part 3~8：征信特征 + 标签 + 马消（马消表仍为同事 5103 口径，left join）
```

重跑：`drop_all_jcr_tables_20260715.sql`
