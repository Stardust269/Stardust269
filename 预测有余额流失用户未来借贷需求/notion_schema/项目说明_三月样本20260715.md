# 项目说明：三月样本（20260715）

> 同事参考脚本：`sql/yye_pril_bal_sample_reference_20260715.sql`  
> 我方征信特征脚本：`sql/run_all_20260715.sql`  
> 十月单 cohort 5401 版：`sql/run_all_20260623.sql`（**勿混用**）

---

## 一、两条流水线对照

| 维度 | 20260623（已验证） | 20260715（三月版） |
|------|-------------------|-------------------|
| 样本月份 | 2025-10 单月 | 2025-08 ~ 2025-10 三月 |
| 样本量 | cohort **5401** | cohort 约 **1.6w**（5401×3，同口径） |
| 源表 | `ayh_mkt_yx_cust_type_union_df`（T-2） | `ayh_mkt_yx_cust_type_base_df`（T-1） |
| rk 分区 | `uuid, user_id` | `uuid, user_id, substr(dt,1,6)`（按月） |
| had/with 锚点 | `days_dt_1` | `days_dt_1`（**与 0623 一致**） |
| cohort 条件 | **5 条件**（含 had） | **与 0623 相同 5 条件** |
| 马消特征 | 无 | left join 同事 `ayh_feature_*` |

---

## 二、cohort 五条件（与 0623 完全一致）

```sql
crdt_lim_yx >= 20000
AND had_0_30_zx = 1
AND had_31_60_zx = 1
AND no_balance_flg_60 = 1
AND with_0_30 + with_31_60 = 0
```

**十月对照验证**（`yye_pril_bal_info_20260623_2`）：

| 条件数 | 结果 |
|--------|------|
| 3 条件（无 had，同事马消 label 口径） | **386,311** |
| 5 条件（含 had，征信 cohort 口径） | **5,401** |

同事三句话里「with+no_balance 子集」指**马消特征表的 3 条件 label**，不是征信 cohort 的全部条件。

---

## 三、核验 SQL

```sql
select m, count(1) as cohort_cnt
from lj_iceberg.ai_decision_dev.jcr_cohort_20260715
group by m order by m;

select count(1) from lj_iceberg.ai_decision_dev.jcr_cohort_20260715;  -- 预期 ~1.6w
```

---

## 四、执行顺序（推荐分 Part 提交，防 OOM）

```text
Part 0~2：0a → 0b → 0pf → 0c-had → 0c-with → merge → cohort（~1.6w）
Part 3~4：Step 1~4（征信底表，已限制 cohort uuid）
Part 5~8：Step 5~8（特征 + 标签 + 马消）
```

重跑清理：`drop_all_jcr_tables_20260715.sql`
