# 项目说明：三月样本（20260715）

> 同事参考：`sql/yye_pril_bal_sample_reference_20260715.sql`（示例，含 `*_5103`，我方不用）  
> 我方实现：`sql/run_all_20260715.sql`  
> 十月核验：`sql/verify_funnel_oct_20260715.sql`

---

## 一、三步漏斗（10月为例，已确认口径）

| 步骤 | 条件 | 人数 | 代码 |
|------|------|------|------|
| **①** | 10月 5103 有余额 + `crdt>=2w` + 月内 **最低额度利用率日** rk=1 | ~5,022,793 | `raw` 表 `rk=1, m='202510'` |
| **②** | **days_dt** 起 0~60 天 **全渠道**变无余额 | ~386,311 | `jcr_pril_bal_pf` |
| **③** | **days_dt_1** 起 0~30 / 31~60 有征信 + 全渠道未提现 | ~5,401 | `jcr_cohort` |

### 锚点（已锁定）

| 用途 | 锚点 | 说明 |
|------|------|------|
| 月内观测日 rk=1 | `days_dt` | `order by pril_bal/crdt_lim_yx` 最小（与同事一致） |
| 无余额 `no_balance_flg_*` | `days_dt` | 从最低利用率日起算 |
| 征信 `had_*_zx` | **`days_dt_1`** | `date_sub(days_dt,1)`，**0715 口径** |
| 提现 `with_*` | **`days_dt_1`** | 同上 |
| 征信特征 / 标签 | `days_dt_1` | 与 0623 一致 |

### cohort 五条件

```sql
no_balance_flg_60 = 1
AND had_0_30_zx = 1 AND had_31_60_zx = 1
AND with_0_30 + with_31_60 = 0   -- 全渠道未提现，保留
```

**不用**：`no_balance_flg_*_5103`、`with_*_5103`

---

## 二、核验 SQL

```sql
-- 跑完 run_part01 后执行 verify_funnel_oct_20260715.sql，或：
select count(1) from jcr_pril_bal_info_raw_20260715 where rk=1 and m='202510';  -- ~5022793
select count(1) from jcr_pril_bal_pf_20260715 where m='202510';                 -- ~386311
select count(1) from jcr_cohort_20260715 where m='202510';                        -- ~5401
```

---

## 三、执行顺序

```text
drop_all → run_part01（验漏斗）→ run_all Part3~7 → run_mx_feature → Part8
```
